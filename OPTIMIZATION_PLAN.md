# pwsh_shortcut_alias 优化文档

## 1. 文档目的

本文档用于沉淀 `pwsh_shortcut_alias` 项目的后续优化方案，目标不是一次性重构，而是基于现有代码风格，按最小改动、可验证、可回退的方式逐步优化。

优化优先级遵循以下原则：

1. 先解决重复规则和职责分散问题。
2. 先优化主链路，再处理外围安装脚本。
3. 每一步都要保证现有 `add / remove / search / update` 行为可回归验证。
4. 不做与当前目标无关的重构，不为了“拆分而拆分”新增过多 helper 函数。

## 2. 当前项目现状

当前仓库核心文件如下：

- [pwsh_shortcut_alias.psm1](/C:/Users/Sonoff_yzy/workspace/github/pwsh_shortcut_alias/pwsh_shortcut_alias.psm1:1)
- [Private/alias_yaml.ps1](/C:/Users/Sonoff_yzy/workspace/github/pwsh_shortcut_alias/Private/alias_yaml.ps1:1)
- [Public/Use-ShortcutAlias.ps1](/C:/Users/Sonoff_yzy/workspace/github/pwsh_shortcut_alias/Public/Use-ShortcutAlias.ps1:1)
- [build.ps1](/C:/Users/Sonoff_yzy/workspace/github/pwsh_shortcut_alias/build.ps1:1)

当前功能能正常覆盖基本需求，但已经出现以下架构摩擦：

1. 别名生命周期规则分散在 `psm1` 和 `alias_yaml.ps1` 之间。
2. URL 判断、路径规范化、存在性检查在多个动作中重复出现。
3. `update` 动作直接把启动行为写进动态函数，后续难以测试和扩展。
4. 安装脚本 `build.ps1` 目前可维护，但后续如果继续扩展安装/卸载逻辑，容易继续变浅。

## 3. 优化目标

### 3.1 主目标

把“快捷别名”的核心职责收拢，让外部命令只表达动作，不直接处理过多实现细节。

### 3.2 具体目标

1. 让别名增删查改围绕一套统一规则工作。
2. 让 YAML 结构、路径规范化、目标校验尽量只在一个地方维护。
3. 让启动行为形成明确边界，降低后续测试和扩展成本。
4. 保持当前使用方式不变：
   - `Use-ShortcutAlias add`
   - `Use-ShortcutAlias remove`
   - `Use-ShortcutAlias search`
   - `Use-ShortcutAlias update`
   - `usa`

## 4. 不在本轮处理的内容

以下内容当前不建议优先处理：

1. 不做大规模目录重构。
2. 不把所有逻辑改成类或复杂面向对象结构。
3. 不引入新的持久化格式，仍然保留 YAML。
4. 不优先处理 UI、美化输出、颜色主题之类的外围问题。
5. 不急于拆 `build.ps1`，除非安装逻辑继续明显膨胀。

## 5. 优化路线图

## 阶段一：收拢别名生命周期规则

### 目标

把别名的核心规则收敛成一个更深的“别名注册表”逻辑中心，减少命令层直接操作 YAML 和重复判断。

### 当前问题

在 [pwsh_shortcut_alias.psm1](/C:/Users/Sonoff_yzy/workspace/github/pwsh_shortcut_alias/pwsh_shortcut_alias.psm1:53) 中：

- `Add-ShortcutAlias` 自己判断 URL、自行解析路径。
- `Remove-ShortcutAlias` 自己先读 YAML 再删除。
- `Search-ShortcutAlias` 自己处理输出格式。
- `Update-ShortcutAlias` 再次判断 URL、再次检查路径。

在 [Private/alias_yaml.ps1](/C:/Users/Sonoff_yzy/workspace/github/pwsh_shortcut_alias/Private/alias_yaml.ps1:37) 中：

- 同时维护 YAML 初始化、读取、排序写回、增删操作。

这导致“一个别名从输入到落盘再到注册”的完整流程被拆散，locality 不足。

### 优化方向

1. 明确“别名数据规则”和“命令入口”两层职责。
2. 让命令层只负责接收参数和输出结果。
3. 让别名注册表逻辑统一负责：
   - 目标类型判断（URL / 文件 / 目录）
   - 路径规范化
   - 别名是否存在判断
   - 别名集合读取与写回
   - 为 `update` 提供统一、干净的数据输入

### 建议落地顺序

1. 先定义一组内部统一数据约定。
   - 例如别名项统一表示为 `Name` + `Path` + `IsUrl`
2. 把重复的目标判断逻辑先收敛。
3. 再把 `add/remove/update` 对 YAML 的直接访问逐步改成走统一入口。
4. 最后再看 `search` 的格式化是否也要并入同一逻辑中心。

### 阶段验收标准

1. `add` 和 `update` 不再各自维护一份 URL 判断逻辑。
2. `remove` 不再先在命令层读一次 YAML，再到持久化层删一次。
3. 命令层函数的职责明显变短，主要保留参数处理与输出。
4. 现有 YAML 文件格式保持兼容，不破坏旧用户数据。

### 风险

1. 如果一步到位把所有函数都挪动，容易引入行为回归。
2. 如果抽象过度，可能为了“集中”反而产生新的浅层 helper。
3. 路径规范化时要注意目录、快捷方式、URL 三类输入不能混淆。

## 阶段二：把启动行为做成明确边界

### 目标

收敛 `update` 阶段的目标启动逻辑，减少系统行为直接泄漏到主流程。

### 当前问题

[Update-ShortcutAlias](/C:/Users/Sonoff_yzy/workspace/github/pwsh_shortcut_alias/pwsh_shortcut_alias.psm1:193) 当前会动态生成如下行为：

- 闭包中直接执行 `Start-Process explorer.exe $target`

这会带来几个问题：

1. 启动策略被硬编码在注册阶段。
2. 后续如果要支持更细分的启动方式，改动点会散。
3. 测试时很难只验证“生成了什么行为”，而不真的启动系统进程。

### 优化方向

1. 给“目标启动”建立一个清晰内部边界。
2. 让 `update` 更像“注册别名函数”，而不是同时决定全部启动细节。
3. 后续如果需要测试，可以只验证：
   - 注册函数是否正确关联目标
   - 启动入口是否收到正确目标

### 建议落地顺序

1. 先把 `Start-Process` 集中到一个明确位置。
2. 再决定动态函数里只保留最薄的一层调用。
3. 等第一阶段稳定后，再考虑是否继续为测试预留更明确替身入口。

### 阶段验收标准

1. 启动命令不再散落在动态函数拼装逻辑里。
2. `update` 的核心职责聚焦为“注册”而不是“实现全部启动细节”。
3. 不改变当前用户直接输入别名即可启动目标的使用体验。

### 风险

1. 动态函数和闭包处理不当，可能导致 `$target` 绑定错误。
2. URL 和本地路径在 `explorer.exe` 下的行为需要回归验证。
3. 如果引入额外间接层太多，会让简单场景变复杂。

## 阶段三：整理搜索与输出职责

### 目标

让搜索匹配和输出格式边界更清晰，避免未来搜索规则和展示规则继续缠在一起。

### 当前问题

[Search-ShortcutAlias](/C:/Users/Sonoff_yzy/workspace/github/pwsh_shortcut_alias/pwsh_shortcut_alias.psm1:170) 当前同时负责：

1. 读取数据
2. 模糊匹配
3. 计算对齐宽度
4. 控制台输出

`Format-AliasOutput` 虽然抽出来了，但它仍然偏向展示结构，不是真正的别名领域规则。

### 优化方向

1. 保持搜索逻辑简单，不要过度拆分。
2. 优先把“搜索结果数据”和“控制台输出格式”区分开。
3. 如果第一阶段完成后代码已经足够清晰，这一阶段可以只做小修，不强推重构。

### 阶段验收标准

1. 搜索结果结构更清楚。
2. 输出格式变化不会影响搜索规则本身。
3. 不为了拆分而额外制造多层浅逻辑。

## 阶段四：观察安装脚本是否需要收拢

### 目标

先观望，再决定 `build.ps1` 是否要进入下一轮优化。

### 当前判断

[build.ps1](/C:/Users/Sonoff_yzy/workspace/github/pwsh_shortcut_alias/build.ps1:1) 当前虽然较长，但主流程还算直观：

1. 依赖检查
2. Profile 处理
3. 模块复制
4. 模块导入
5. 卸载清理

目前不建议提前大拆。

### 触发优化的条件

只有当下面情况出现时，再把它列为高优先级：

1. 安装失败恢复逻辑明显增多。
2. 多 PowerShell 环境兼容逻辑继续膨胀。
3. 安装与卸载流程出现大量重复分支。
4. README、安装行为和脚本行为开始出现明显偏差。

### 当前建议

1. 只做必要修补，不做大改。
2. 后续如果继续增强安装能力，再整理成单一安装工作流。

## 6. 推荐执行顺序

建议按以下顺序推进：

1. 阶段一：收拢别名生命周期规则
2. 阶段二：收拢启动行为边界
3. 阶段三：整理搜索与输出职责
4. 阶段四：观察安装脚本，必要时再启动

原因如下：

1. 阶段一收益最大，且直接影响 `add/remove/search/update` 四个核心动作。
2. 阶段二依赖阶段一的收敛结果，过早处理容易重复改。
3. 阶段三属于可选增强，应该在主链路稳定后再做。
4. 阶段四是外围流程，不应抢主链路优先级。

## 7. 每阶段建议的回归检查

每完成一个阶段，至少回归以下场景：

1. 添加文件路径别名
2. 添加目录别名
3. 添加 URL 别名
4. 删除存在的别名
5. 删除不存在的别名
6. 搜索精确命中
7. 搜索模糊命中
8. 更新全部别名
9. 更新时遇到失效路径

建议补充的关注点：

1. YAML 文件首次创建是否正常
2. YAML 别名排序是否保持稳定
3. 旧 YAML 文件是否仍能读取
4. 别名注册后是否能在当前会话直接调用

## 8. 建议新增的配套文档

为了后续优化更顺畅，建议补两份轻量文档：

### 8.1 CONTEXT.md

用于统一项目术语，建议至少定义：

- Shortcut Alias：用户输入的别名
- Target：别名指向的 URL / 文件 / 目录
- Registry：别名集合的内存与持久化表示
- Update：把别名注册为全局函数的动作
- Launch：执行目标启动的动作

### 8.2 简单测试清单

即使暂时不引入完整自动化测试，也建议先补一份手工测试清单，避免每轮优化后靠记忆回归。

## 9. 第一阶段的具体实施建议

如果下一步就开始动代码，建议先做这一小步：

1. 先梳理一个统一的“目标解析”入口。
2. 把 `Add-ShortcutAlias` 和 `Update-ShortcutAlias` 中重复的 URL / 路径判断收敛掉。
3. 暂时不要同时改 `search` 和 `build.ps1`。

这样做的好处是：

1. 改动面最小。
2. 风险最可控。
3. 能最快验证“集中规则”这条路线是否合适。

## 10. 结论

当前项目最值得先优化的不是目录结构，也不是安装脚本，而是“别名生命周期规则”的收拢。

只要先把这条主链路变深，后面的启动边界、搜索职责、安装流程才会更容易稳妥演进。后续建议严格按阶段推进，每次只处理一个主题，避免一次性混改带来回归风险。
